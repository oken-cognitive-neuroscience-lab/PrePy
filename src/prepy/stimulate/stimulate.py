

def raspberry_pi_stimulation(parameters, logger):
    """Raspberry PI Stimulation."""

    # try:
    #     import RPi.GPIO as GPIO
    # except ImportError:
    #     return 'RPi.GPIO not installed'

    logger.info('starting stimulation')
